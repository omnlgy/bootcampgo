info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/logout
  headers:
    - name: Authorization
      value: Bearer ***
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
