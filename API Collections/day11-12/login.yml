info:
  name: login
  type: http
  seq: 0

http:
  method: POST
  url: http://localhost:8080/api/login
  body:
    type: json
    data: |-
      {
        "email": "alice@example.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
